const assert = require('assert');
describe('Simple Math Test', () => {
 it('should return 3', () => {
        assert.equal(1 + 2, 3);
    });
 it('should return 9', () => {
        assert.equal(3 * 3, 9);
    });
});
